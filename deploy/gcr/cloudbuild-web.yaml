steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "${_REGION}-docker.pkg.dev/$PROJECT_ID/cadris/web"
      - "-f"
      - "apps/web/Dockerfile"
      - "."
images:
  - "${_REGION}-docker.pkg.dev/$PROJECT_ID/cadris/web"
substitutions:
  _REGION: "europe-west1"
